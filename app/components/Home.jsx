const React = require('react')
const { Link } = require('react-router')

const Home = (props) => {
    return (
        <div className="flexbox vis-animation">
        
        </div>
    )
}

module.exports = Home